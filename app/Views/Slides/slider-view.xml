<GridLayout loaded="onLoaded" columns="* * *" height="100" backgroundColor="blue">
    <Label text="{{ first }}" col="0"/>
    <Label text="{{ last }}" tap="test" col="1"/>
    <Button text="Remove me" tap="buttonTap" col="2"/>
</GridLayout>
